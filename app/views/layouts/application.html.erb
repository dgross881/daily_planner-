<!DOCTYPE html>
<html>
<head>
  <title>DailyPlanner</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'vendor/modernir' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<% flash.each do |type, message| %>
 <div class="alert-box  text-center alert flash <%= type  %>">
    <%= message %>
    <a href="#" class="close">&times;</a>
 </div>
<% end  %>
<header class="row global-header">
  <div class="small-12 small-centered medium-8 medium-centered large-5 large-centered columns">
    <h1><%= link_to "Daily Planner", root_path %></h1>
    <%= link_to "Todo Lists", todo_lists_path %>
    <% if logged_in? %>
      <%= link_to "Log Out", logout_path, method: :delete %>
    <% else %>
      <%= link_to "Log In", new_user_session_path %>
      <%= link_to "Sign Up", new_user_path %>
    <% end %>
  </div>
</header>
<body>
   <div class="row collapse">
    <div class="small-12 small-centered medium-8 medium-centered large-5 large-centered columns">
      <%= yield %>
    </div> 
  </div>
  <foot class="row">
    <div class="small-12 small-centered medium-8 medium-centered large-5 large-centered columns text-center">
      <% if logged_in? %>
      <%= link_to "Settings", edit_user_path(current_user) %>
      <% end -%>
    </div>
  </foot>   
</body>
</html>
